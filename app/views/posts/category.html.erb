<section class="intro intro--blue">
  <h1 class="intro__title">Category <%= params[:name] %></h1>
  <h2 class="intro__subtitle">"amount" <%= params[:name] %> projects</h2>
</section>

<section class="projects">
  <div class="projects__parent">
    <% @posts.each do |post| %>
      <%= render post %>
    <% end %>
  </div>
</section>
